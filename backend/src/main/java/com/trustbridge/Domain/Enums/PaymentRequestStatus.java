package com.trustbridge.Domain.Enums;

public enum PaymentRequestStatus {
        PENDING,
        PROCESSING,
        PAID,
        FAILED,
        EXPIRED,
        CANCELLED,
        REFUNDED
}
